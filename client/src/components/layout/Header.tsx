import React from 'react';
// import { MdNotifications } from 'react-icons/md';

type Props = {};

const Header = (props: Props) => {
	return (
		<header className='fixed left-0 top-0 right-0 pl-[250px] pr-[40px] z-30 bg-white shadow-sm min-h-[50px] flex items-center '></header>
	);
};

export default Header;
